name: Fortran CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ghcr.io/motezuki/fortran-dockerimage:latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Build application
      run: |
        gfortran -o myapp src/*.f90
